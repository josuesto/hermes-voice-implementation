# Codex process-loopback helper

Source of truth is `companion/process_loopback/`. This directory is a development wrapper so existing helper commands keep working.

```powershell
.\companion\process_loopback\build-helper.ps1
.\prototype\process-loopback\build-helper.ps1
```
